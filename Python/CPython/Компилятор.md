После того, как был исходный код прошел парсинг (имеется дерево AST - операции, классы, функции, пространства имен) его необходимо преобразовать в инструкции понятные для процессора (Python виртуальной машины).

```
(Модуль чтения) --текст--> 
(Модуль лексического анализа) --CST-->
(Парсер) --AST-->
(Компилятор) --CFG-->  👈🏻
(Ассемблер) --Байткод-->  👈🏻
(Выполнение)
```
В компиляции учавствуют два компонента:
- **Компилятор**. Преобразование AST к графу потока управления CFG (Control Flow Graf) представляющий логическую последовательность выполнения (какое отличие от AST?)
- **Ассемблер**. Преобразование CFG к инструкциям байт-кода

Входная точка компиляции функция `PyAST_CompileObject` (`Python/compile.c`)