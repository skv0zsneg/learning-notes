После того, как была собрана вся конфигурация исходный код должен быть разобран для дальнейшей компиляции байт кода. Этот процесс разбит в несколько этапов.

```
(Модуль чтения) --текст--> 
(Модуль лексического анализа) --CST-->  👈🏻
(Парсер) --AST-->  👈🏻
(Компилятор)
```

### Модуль лексического анализа

**Лексический анализ**
Текст кода разбивается на токены - слова и символы (`if`, `+`, `42`, `'hello'`)

**Синтаксический анализ**
Построение структуры дерево из полученных выше токенов для отражения программы. 

В CPython (здесь и везде далее для версии 3.9) эти шаги выполняются в функции `PyParser_ParseFileObject` (`Parser/parsetok.c`)
	1. Создается экземпляр состояния токенизатора в функции `PyTokenizer_FromFile` (`Parser/tokenizer.c`)
	2. Этот токенизатор переходит в функцию `parsetok` (`Parser/parsetok.c`)
		1. С помощью экземпляра токенизатора получается следующий токен через функцию `tok_get` (`Parser/tokenizer.c`) которая вызывается через `PyTokenizer_Get` (`Parser/tokenizer.c`)
		2. Далее из токена собирается нода и кладется в дерево CST
		3. 1 и 2 продолжается пока курсор экземпляра токенизатора не дойдет до конца строки

![[Pasted image 20251225000224.png]]

